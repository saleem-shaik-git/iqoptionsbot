# IQ Option AI Dashboard

Next.js/React frontend scaffold. Dashboard features are implemented in later phases.

## Development

```bash
npm install
npm run dev
```

The frontend expects the backend at `NEXT_PUBLIC_API_URL` and the WebSocket endpoint at `NEXT_PUBLIC_WS_URL`.
