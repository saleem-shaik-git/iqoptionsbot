# Presentation Layer

FastAPI routers, request/response schemas, authentication dependencies, WebSocket endpoints, exception handlers, and HTTP concerns only. Business logic belongs in application services.
